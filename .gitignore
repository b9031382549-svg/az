*.log
.DS_Store
.env
.env.backup
.env.bak.*
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

# WSL/Windows download artifacts
*:Zone.Identifier
# Local sqlite created by scaffolding (project uses Postgres)
/database/*.sqlite

# Throwaway root-level test scripts
/_*.php

# Synonym enrichment work files
/storage/app/catalog_codes.jsonl
/storage/app/catalog_synonyms.jsonl

# Internal docs / reports (not shipped)
/docs/
/ByFedor/
/system-snapshot-2026-07-08/

# Production DB dumps (huge + sensitive — never commit)
/prod-db-*.dump
/*.dump

# Downloaded reference/training data (multi-GB — real customs HS datasets, never commit)
/research-data/

# Local benchmark/gold/training data dump (not shipped)
/123/

# Local MLX/LoRA research prototype (separate project, not part of this app)
/AZdata-main/

# Raw gold-reference CSV export (19MB+ — real customs HS dataset, never commit; imported
# via `benchmark:import-gold --gold-ref=`, SCP'd to the server when needed, not shipped).
/team_export_*/
